{
  "name": "culinary-journal-web",
  "version": "1.0.0",
  "private": true,
  "description": "Mobile-first personal recipe journal web app with auth, SQLite, uploads, and admin views.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check app.js && node --check server.js"
  },
  "engines": {
    "node": ">=24 <25"
  }
}
