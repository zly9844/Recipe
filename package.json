{
  "name": "culinary-journal-web",
  "version": "1.0.0",
  "private": true,
  "description": "手机端私房菜谱网页应用，包含移动端记录界面、步骤图片上传、后端 API 和后台管理。",
  "scripts": {
    "start": "node server.js",
    "check": "node --check app.js && node --check server.js"
  },
  "engines": {
    "node": ">=24"
  }
}
